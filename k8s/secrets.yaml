# ============================================================
# WARNING: DO NOT COMMIT REAL SECRET VALUES TO THIS FILE.
# This file contains placeholder values only.
# Apply the real secret manually before deploying:
#
#   kubectl -n team-manager create secret generic team-manager-secrets \
#     --from-literal=db-name=<real-db-name> \
#     --from-literal=db-user=<real-db-user> \
#     --from-literal=db-password=<real-db-password> \
#     --from-literal=google-client-secret=<real-google-client-secret> \
#     --dry-run=client -o yaml | kubectl apply -f -
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: team-manager-secrets
  namespace: team-manager
type: Opaque
stringData:
  db-name: REPLACE_ME
  db-user: REPLACE_ME
  db-password: REPLACE_ME
  google-client-secret: REPLACE_ME

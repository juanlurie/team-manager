apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  namespace: team-manager
spec:
  selector:
    app: mcp-gateway
  ports:
    - port: 8765
      targetPort: 8765
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-gateway
  namespace: team-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-gateway
  template:
    metadata:
      labels:
        app: mcp-gateway
    spec:
      containers:
        - name: mcp-gateway
          image: ghcr.io/juanlurie/team-manager-mcp:b8a2e94d41fbda5e1a427dd28b68a5e47c2d3ae4
          ports:
            - containerPort: 8765
          env:
            - name: TEAM_MANAGER_API_URL
              value: http://api:5000
            - name: MCP_PORT
              value: "8765"

import { Routes } from '@angular/router';
import { FeaturePermissionsComponent } from './feature-permissions.component';

export const FEATURE_PERMISSIONS_ROUTES: Routes = [
  { path: '', component: FeaturePermissionsComponent }
];
